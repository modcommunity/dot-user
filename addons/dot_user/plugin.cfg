[plugin]

name="dot-user"
description="Player profiles that follow a person between servers: pseudonymous per-scope identity, display-name validation, pluggable storage (local JSON, memory, or an open HTTP backbone), and a resolve-on-join cache that never touches the network on the join path."
author="dot"
version="0.1.0"
script="dot_user_plugin.gd"
